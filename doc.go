// package gmock is an HTTP mock server library that can receive and respond to HTTP requests dinamically.
// The server is configurable through HTTP endpoints that allow to add and list stubs at runtime
// and can also preload stubs from configurable directories.
// Can work with both JSON and YAML stubs.
package gmock
